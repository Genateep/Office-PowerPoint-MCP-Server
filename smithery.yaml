mcpServers:
  powerpoint:
    label: "Office PowerPoint MCP Server"
    transport: "streaming-http"
    hosted:
      startCommand:
        type: http
        port: 8000      # порт, на котором слушает сервер внутри контейнера
        path: /          # или /mcp, если сервер слушает именно там
